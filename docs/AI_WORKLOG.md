# AI Worklog

Этот файл — источник данных для обязательной вкладки AI Worklog в приложении.
Перед сдачей информация должна быть перенесена или импортирована в интерфейс.

## Сводка

- AI-инструмент: Codex.
- Общее время: **заполняется владельцем по фактическому таймеру**.
- Токены: интерфейс текущей сессии не предоставляет надёжное итоговое число;
  не подсчитывались.
- Период работы: начат 15 сентября 2026 года.

## Этап 1. Анализ и подготовка репозитория

- Человек: предоставил исходное задание, запросил максимально быстрый реальный
  план, выбрал React Context и CSS Modules.
- AI: прочитал RTF, сравнил варианты, предложил вариант с порталами, сформировал
  спецификацию, архитектуру, план, чеклисты, инструкции для следующих AI-сессий
  и запускаемый каркас.
- Суть промптов: «изучи задание и составь план»; «сократи график до максимально
  быстрого реального срока»; «выбери React Context и CSS Modules, запиши план в
  файл и подготовь репозиторий для AI-driven разработки».
- Решения человека: использовать React Context; использовать CSS Modules;
  подготовить процесс AI-driven разработки до реализации функций.
- Исправления и отклонённые предложения: пятидневный график был заменён на
  реалистичную оценку 12–16 часов. AI также ошибочно сопоставил ответ `npm view`
  для Vite с версией TypeScript; ошибка была обнаружена первой установкой,
  версия перепроверена отдельным запросом и исправлена до продолжения работы.
- Проверки: `npm run typecheck`, `npm run test:run` и `npm run build` успешно
  выполнены; стартовый smoke-тест — 1 из 1 пройден.
- Фактическое время этапа: **заполняется владельцем**.

## Этап 2. Уточнение доменной модели

- Цель: зафиксировать новую модель миров, порталов, времени жизни, риска и
  статусов до начала реализации.
- Человек сделал: определил миры отдельными сущностями, случайные параметры
  портала, лимит 20 открытых порталов, формулы рассеивания и времени, риск как
  долю истёкшего времени и терминальное закрытие после критичного состояния.
- AI сделал: согласовал формулировки между спецификацией, архитектурой, планом и
  чеклистом; явно описал границы диапазонов и математические крайние случаи.
- Суть ключевого промпта: «зафиксируй изменения модели миров и порталов, формулы
  времени и риска и правила статусов».
- Что было принято или изменено вручную: все перечисленные продуктовые правила
  предоставлены человеком.
- Где AI ошибся или предложил слабое решение: прежняя формула напрямую повышала
  риск от энергии, а роль наблюдателя не имела определённого доменного эффекта;
  обе идеи исключены из актуальных правил до отдельного согласования действий.
- Проверки: проверена непротиворечивость изменённых документов; код не менялся;
  `npm run typecheck`, `npm run test:run` (1 тест) и `npm run build` выполнены
  успешно.
- Фактическое время: **заполняется владельцем**.
- Токены, если доступны: надёжная статистика недоступна.

## Этап 3. Проект механики стабилизации

- Цель: добавить осмысленные возможности для важных опасных и критичных
  порталов и создать выбор при ограниченном числе попыток.
- Человек сделал: сформулировал критерии важности, потребность в наблюдателе,
  ограничении попыток, случайном результате и запрете уменьшать время жизни;
  отдельно запретил отправку наблюдателя в критичный портал при любых условиях.
- AI сделал: предложил общий лимит три попытки за скользящий час, формулу шанса,
  постоянный случайный бонус к стабильности и три альтернативных алгоритма;
  добавил требования к интерфейсу и тестам.
- Суть ключевого промпта: «добавь возможности для опасных и критических
  порталов и предложи неочевидные алгоритмы стабилизации».
- Что было принято или изменено вручную: продуктовые критерии, инвариант
  безопасной попытки и абсолютный запрет наблюдателя для критичного портала
  заданы человеком; позднее числовые коэффициенты были приняты как стартовый
  баланс с требованием быстрой настройки.
- Где AI ошибся или предложил слабое решение: первоначальный проект AI разрешал
  наблюдателя очень важному критичному порталу; человек исправил это правило.
  Формула вероятности была принята только как легко настраиваемая стартовая
  версия и требует проверки игровыми прогонами.
- Проверки: `git diff --check`, `npm run typecheck`, `npm run test:run` (1 тест)
  и `npm run build` выполнены успешно; код приложения не менялся.
- Фактическое время: **заполняется владельцем**.
- Токены, если доступны: надёжная статистика недоступна.

## Этап 4. Уточнение модели времени

- Цель: закрыть вопросы расходования энергии и начального времени при нулевом
  рассеивании.
- Человек сделал: принял предложенные правила для пунктов 1 и 2 аудита
  готовности к разработке.
- AI сделал: зафиксировал `C = 10 секунд`, секундный пересчёт энергии, обновление
  `D` и `S` раз в 60 секунд и отложенную инициализацию `T_initial`.
- Суть ключевого промпта: «пункты 1 и 2 приняты».
- Что было принято или изменено вручную: оба решения подтверждены человеком без
  изменений.
- Где AI ошибся или предложил слабое решение: нет данных.
- Проверки: `git diff --check`, `npm run typecheck`, `npm run test:run` (1 тест)
  и `npm run build` выполнены успешно; код приложения не менялся.
- Фактическое время: **заполняется владельцем**.
- Токены, если доступны: надёжная статистика недоступна.

## Этап 5. Проект исследовательского цикла

- Цель: превратить наблюдение за порталами в короткую игровую сессию с целью,
  ограниченным ресурсом и последствиями решений.
- Человек сделал: предложил начальный штат сотрудников, исследование новых
  миров, влияние переходов на портал, ускорение дополнительными сотрудниками,
  цикл 5–15 минут и случайное, а не расписанное появление миров; уточнил, что
  исчерпывающая портал отправка допустима при наличии второго надёжного портала.
- AI сделал: предложил 10-минутный цикл, 6 миров, 12 сотрудников, группы 1–4,
  формулы цены перехода и скорости исследования, случайный директор с защитой
  от серии, возврат сотрудников и итоговые метрики.
- Суть ключевого промпта: «доработай игровую логику исследования миров и потерь;
  миры должны открываться случайно».
- Что было принято или изменено вручную: направление игрового цикла и случайное
  появление миров заданы человеком; оператор вручную закрывает порталы в
  исследованные миры, чтобы освобождать лимит 20; схлопнувшийся портал тоже
  занимает слот до явного закрытия оператором; позднее числовой баланс принят
  как стартовый и вынесен в единую конфигурацию.
- Где AI ошибся или предложил слабое решение: сначала AI упомянул расписание
  появления миров и безусловно блокировал отправку, исчерпывающую энергию;
  человек уточнил случайное появление и исключение для резервного маршрута.
- Проверки: `git diff --check`, `npm run typecheck`, `npm run test:run` (1 тест)
  и `npm run build` выполнены успешно; код приложения не менялся.
- Фактическое время: **заполняется владельцем**.
- Токены, если доступны: надёжная статистика недоступна.

## Этап 6. Закрытие продуктовых решений перед разработкой

- Цель: устранить оставшиеся неоднозначности игрового цикла и подготовить
  проверяемые требования к реализации.
- Человек сделал: принял аварийную эвакуацию через критичный портал, полную
  паузу симуляции в неактивной вкладке, стартовый баланс, параметры
  стабилизации и роль наблюдателя; определил завершение строго по таймеру и
  потребовал сохранять результаты между партиями.
- AI сделал: собрал все числа в проект `GameBalanceConfig`, описал принудительное
  схлопывание портала после критической эвакуации, правила заморозки завершённой
  партии и версионированную историю результатов; обновил план и чеклист.
- Суть ключевого промпта: «решения приняты; на неактивной вкладке поставить
  симуляцию на паузу, оставить быстрый способ менять баланс, завершать по
  таймеру и сохранять результаты».
- Что было принято или изменено вручную: все шесть решений перечислены
  человеком явно; значения баланса считаются стартовыми, а не окончательными.
- Где AI ошибся или предложил слабое решение: прежний критерий сравнения по
  оставшемуся времени противоречил завершению всех партий по одному таймеру;
  он заменён на число схлопнувшихся порталов.
- Проверки: `git diff --check`, `npm run typecheck`, `npm run test:run`
  (1 тест) и `npm run build` выполнены успешно; код приложения не менялся.
- Фактическое время: **заполняется владельцем**.
- Токены, если доступны: надёжная статистика недоступна.

## Этап 7. Реализация доменной модели

- Цель: выполнить этап 1 плана как набор чистых, детерминированно проверяемых
  правил без зависимости от React.
- Человек сделал: поручил приступить к этапу 1; числовой баланс и инварианты
  были утверждены им ранее.
- AI сделал: добавил типизированную конфигурацию, сущности, физику порталов,
  случайный директор, экспедиции, исследование, стабилизацию, наблюдателей,
  ручное закрытие, завершение по таймеру и воспроизводимый демосценарий;
  обновил чекбоксы плана и создал доменные тесты.
- Суть ключевого промпта: «приступай к этапу 1».
- Что было принято или изменено вручную: новые решения от человека на этом этапе
  не запрашивались; реализованы ранее согласованные правила.
- Где AI ошибся или предложил слабое решение: первый проход симуляции создавал
  не более одного портала за большой `deltaSeconds`; заменён на последовательные
  тики, добавлен тест нескольких появлений. Исправлен тип кортежей в конфигурации.
- Проверки: `npm run typecheck`, `npm run test:run` (39 тестов в 6 файлах),
  `npm run build` и `git diff --check` выполнены успешно; ручной игровой прогон
  невозможен до интеграции состояния и интерфейса на этапах 2–3.
- Фактическое время: **заполняется владельцем**.
- Токены, если доступны: надёжная статистика недоступна.

## Этап 8. Состояние приложения и локальное сохранение

- Цель: выполнить этап 2 — связать доменную модель с Context/useReducer,
  управлять симуляцией и переживать перезагрузку страницы.
- Человек сделал: поручил реализацию этапа 2; новых продуктовых правил не
  вводил.
- AI сделал: добавил `PortalProvider`, типизированные actions и reducer,
  паузу скрытой вкладки, уведомления, выбор/фильтр, независимые версионированные
  сохранения текущей партии и истории, восстановление результата по `id`,
  маркер подтверждённой очистки и безопасный демосценарий. Подключил Provider
  к корню приложения и добавил тесты reducer, хранения и взаимодействия.
- Суть ключевого промпта: «приступай к реализации этапа 2».
- Что было принято или изменено вручную: новых ручных правок на этом этапе нет;
  реализованы решения, зафиксированные ранее.
- Где AI ошибся или предложил слабое решение: первый прогон тестов Provider
  оставил DOM первого теста перед вторым; добавлена явная очистка после каждого
  теста. Для подтверждённой очистки истории потребовался маркер, иначе итог
  текущей партии появлялся бы снова после перезагрузки.
- Проверки: `git diff --check`, `npm run typecheck`, `npm run test:run`
  (54 теста в 9 файлах) и `npm run build` выполнены успешно. Ручной игровой
  сценарий остаётся этапом 3, поскольку игровой интерфейс ещё не создан.
- Фактическое время: **заполняется владельцем**.
- Токены, если доступны: надёжная статистика недоступна.

## Этап 9. Основной интерфейс

- Цель: выполнить этап 3 плана и связать игровой сценарий с существующим состоянием.
- Человек сделал: поручил продолжить проект и приступить к этапу 3; новых
  продуктовых правил не вводил.
- AI сделал: собрал адаптивный экран партии, портал с прогнозом перехода и
  действиями, сводку миров, журнал, итоги и историю результатов; добавил
  навигацию и тесты основных взаимодействий.
- Суть ключевого промпта: «продолжай работу над проектом. приступай к этапу 3».
- Что было принято или изменено вручную: на этом этапе ручных правок нет.
- Где AI ошибся или предложил слабое решение: выявленных ошибок первого
  прогона нет; полноценный ручной прогон и проверка ширины 320 px остаются
  отдельными задачами этапа 5.
- Проверки: `npm run typecheck`, `npm run test:run` (56 тестов в 9 файлах),
  `npm run build`, `git diff --check`.
- Фактическое время: **заполняется владельцем**.
- Токены, если доступны: надёжная статистика недоступна.

## Этап 10. Пограничные состояния интерфейса

- Цель: выполнить этап 4 и сделать редкие состояния доступными для проверки.
- Человек сделал: поручил приступить к этапу 4; новых правил не вводил.
- AI сделал: добавил чистые правила причин блокировки действий, критическое
  объяснение риска, пустое состояние и восстановление демоданных, проверочные
  сценарии, ускорение времени, Error Boundary и тесты.
- Суть ключевого промпта: «приступай к этапу 4».
- Что было принято или изменено вручную: ручных правок на этом этапе нет.
- Где AI ошибся или предложил слабое решение: первая версия сценария лимита
  сохраняла причину закрытия у активных клонов портала; проверка LocalStorage
  обнаружила несогласованность, поле исправлено. Первый тест интерфейса выбирал
  кнопку навигации вместо строки портала; селектор теста уточнён.
- Проверки: `npm run typecheck`, `npm run test:run`, `npm run build`,
  `git diff --check`. Ручные сценарии и проверка доступности относятся к этапу 5.
- Фактическое время: **заполняется владельцем**.
- Токены, если доступны: надёжная статистика недоступна.

## Этап 11. Проверки

- Цель: выполнить этап 5 плана и зафиксировать реальные результаты проверки.
- Человек сделал: поручил приступить к этапу 5; новых продуктовых правил не
  вводил.
- AI сделал: добавил девять проверочных тестов и тест фокуса, прошёл семь
  сценариев в Chrome Headless через DevTools, проверил 320 и 1440 px, клавиатуру,
  системное подтверждение и паузу вкладки. Заполнил `docs/TEST_CHECKLIST.md`
  и подробный `docs/TEST_RUN_STAGE5.md`.
- Суть ключевого промпта: «приступай к этапу 5».
- Что было принято или изменено вручную: ручных правок владельца на этом этапе
  нет. AI исправил ограничение ширины выбора сценария, возврат фокуса после
  эвакуации и проверочный сценарий исследованного мира.
- Где AI ошибся или предложил слабое решение: первый браузерный прогон
  ошибочно выбрал активную строку вместо схлопнувшейся; сценарий теста исправлен.
  Позже проверка чисто браузерного перехода выявила потерю фокуса после
  размонтирования кнопки. Нативный диалог однажды заблокировал тестовый сеанс;
  новый сеанс позволил проверить подтверждение и фокус.
- Проверки: `npm run typecheck`, `npm run test:run` (75 тестов в 12 файлах),
  `npm run build`, `git diff --check` и браузерный прогон прошли успешно.
  Чистая установка и публичный запуск остаются этапом 6.
- Фактическое время: **заполняется владельцем**.
- Токены, если доступны: надёжная статистика недоступна.

## Шаблон следующей записи

### Этап N. Название

- Цель:
- Человек сделал:
- AI сделал:
- Суть ключевого промпта:
- Что было принято или изменено вручную:
- Где AI ошибся или предложил слабое решение:
- Проверки:
- Фактическое время:
- Токены, если доступны:

## Решения, которые нужно описать перед сдачей

Добавить 3–5 итоговых авторских решений, реальные ошибки AI, ручные доработки,
способ проверки и список улучшений для коммерческой версии. Не заполнять эти
пункты вымышленными примерами.
